{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev --stream",
    "build": "lerna run build --stream",
    "lint": "lerna run lint",
    "format": "lerna run format"
  },
  "dependencies": {
    "next": "15.1.4",
    "react": "19",
    "react-dom": "19"
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  }
}
